[string]

# Lib name
libname = hand

# Prefix of the tester to be called
testerprefix = hand

legendname = Handwritten fixed (gcc)

[code]

cflags-vec = '-O2 -ftree-vectorize -fstrict-aliasing -mavx -mfma -D__int64=int64_t'
# Marker for the plot
marker = 'v'
color = '#202020'
lw = 2.
